services:
  - type: web
    name: explore-md-pair
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MEGA_EMAIL
        sync: false
      - key: MEGA_PASSWORD
        sync: false
